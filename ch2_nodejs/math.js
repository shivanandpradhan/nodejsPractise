
const add = (x, y) => x + y;
const sub = (x, y) => x - y;
const mul = (x, y) => x + y;
const div = (x, y) => x / y;
const rem = (x, y) => x % y;
const pow2 = x => x * 2;


module.exports = {add, sub, mul, div, rem, pow2}